# Core dependencies (existing)
aiogram==3.1.1
anthropic==0.20.0
aiosqlite==0.19.0
numpy>=2.0.0
pandas==2.2.3
requests==2.31.0
yfinance==0.2.36
google-generativeai==0.3.1
groq==0.3.0
borsapy>=0.8.0
playwright==1.42.0
python-dotenv==1.0.1

# ✅ NEW: Monitoring
aiohttp~=3.8.5              # Health check server için (aiogram uyumlu)
prometheus-client>=0.19.0  # Metrics (opsiyonel, kendi collector'ımız var)

# ✅ NEW: Config & Validation
pydantic>=1.10.0,<2.0   # Settings validation (v1 for BaseSettings compatibility)

# ✅ NEW: Security & Utilities
python-jose[cryptography]>=3.3.0  # JWT (future auth)
cryptography>=41.0.0    # Security primitives

# ✅ NEW: Development
pytest>=7.4.0           # Testing
pytest-asyncio>=0.21.0  # Async test support
mypy>=1.6.0            # Type checking
ruff>=0.1.0            # Fast linter
